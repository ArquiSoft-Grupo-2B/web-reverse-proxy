services:
  nginx:
    image: nginx:latest
    container_name: reverse-proxy
    ports:
      - "8555:8555"
    networks:
      - frontend_net
      - public_net

networks:
  public_net:
    external: true
  frontend_net:
    external: true
